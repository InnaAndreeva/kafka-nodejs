CREATE TABLE cards (
  id        		varchar(36) NOT NULL,
  cardNumber  		varchar(16) NOT NULL,
  expirationDate 	varchar(10) NOT NULL,
  cardImgUrl		varchar(600) NOT NULL,
  cv				varchar(3) NOT NULL,
  cardHolder  		varchar(255) NOT NULL
);
